import java.util.Arrays;
import java.util.List;

public class CamelCaseString {
	
	public static void main(String args[]) {
		
		String s = "java is programing language";
		String splited[] = s.split(" ");
		List<Integer> list = Arrays.asList(1,2,1,3,2,3);
		/*
		 * StringBuilder sb = new StringBuilder(); for(String word: splited) { char
		 * firstChar = word.charAt(0); if(!Character.isUpperCase(firstChar)) {
		 * sb.append(Character.toUpperCase(firstChar)).append(word.substring(1));
		 * 
		 * } else sb.append(word.substring(0)); } String result = sb.toString().trim();
		 * System.out.println(result);
		 */
		
		/*for(String str : splited) {
			String[] str3 = str.toCharArray();
			
		*/
		
		for(int i=0; i<list.)
		
		
		
		
		
		}
		
	}

}
